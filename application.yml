management:
  security:
    enabled: false

security:
  basic:
    enabled: false

spring:
  cloud:
    services:
      registrationMethod: direct

logging:
  level:
    io:
      pivotal:
        training: INFO
